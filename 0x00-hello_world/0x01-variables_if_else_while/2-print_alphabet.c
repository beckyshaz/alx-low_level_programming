#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (success)
 * print alphabet a-z
 */
int main(void)
{
	char ch = 'a';

	while (ch <= 'z')

	{
		putchar(ch);
		ch++;
		putchar('\n');

	}









return (0);

}
