#include "main.h"
/**
 *rev_string -  function that reverses a string
 *@s: string to be reversed
 *Return: Always 0
 */
void rev_string(char *s)
{
	int i;
	int length = 0;

	for (i = 0; s[i] != '\0'; i++)
		length++;
	for (i = length; i >= 0; i--)
}
