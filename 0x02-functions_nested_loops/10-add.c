#include "main.h"
/**
 *add - function that adds two integers
 *@k: integer being added
 *@x: integer being added
 *Return: Always 0
 */
int add(int k, int x)
{
	int sum;

	sum = (k + x);
	return (sum);
}



