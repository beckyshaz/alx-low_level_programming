#include "main.h"
/**
 *mul - function that multiplies two integers
 *@a: interger being multiplied
 *@b: interger being multiplied
 *Return: Always 0
 */
int mul(int a, int b)
{
	int multi;

	multi = (a * b);
		return (multi);
}
